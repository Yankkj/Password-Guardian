# src/utils.py

def clean_input(text: str) -> str:
    """
    Função utilitária para limpar strings de entrada (remover espaços extras, etc.).
    Pode ser expandida conforme a necessidade do projeto.
    """
    return text.strip()